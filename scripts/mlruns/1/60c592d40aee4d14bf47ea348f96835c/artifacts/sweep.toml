# Sweep processing methods grid
# Tests all combinations of cleaner, imputer, aggregator, transformer

[cleaner]
method = ["default"]

[imputer]
method = ["default", "simple"]

[aggregator]
method = ["detailed"]

[transformer]
encoding = ["onehot", "label", "none"]
